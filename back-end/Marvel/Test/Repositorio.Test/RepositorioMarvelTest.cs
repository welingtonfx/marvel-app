using AutoFixture;
using Infra.Repositorio;
using Moq;
using Newtonsoft.Json;
using RestSharp;
using System;
using System.Collections.Generic;
using System.Net;
using System.Text;
using System.Threading;
using System.Threading.Tasks;
using Xunit;

namespace Test.Repositorio.Test
{
    public class RepositorioMarvelTest
    {
        //private readonly Fixture _fixture;
        //private readonly RepositorioMarvel _repositorioMarvel;

        //private readonly Mock<IRestClient> _restClient;
        //private readonly Mock<IRestRequest> _restRequest;
        



        //public RepositorioMarvelTest()
        //{
        //    _fixture = new Fixture();
        //    _repositorioMarvel = new RepositorioMarvel();

        //    _restClient = new Mock<IRestClient>();
        //    _restRequest = new Mock<IRestRequest>();

        //}

        //[Fact]
        //public async Task Obter()
        //{
        //    var request = _fixture.Create<RestRequest>();

        //    _restClient.Setup(f => f.ExecuteAsync(request, Method.GET, It.IsAny<CancellationToken>()).Result;
        //}
    }
}
